apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - port: 5001
      targetPort: 5001
